Running GalPrime
================


Running the `galprime` routine is relatively straightforward.

You will need a configuration file (to get started with this, 
call `galprime.dump_default_config_file()`).

